{
  "name": "plataforma-quantitativa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:frontend && npx tauri dev",
    "build": "npm run build:frontend && npx tauri build",
    "build:frontend": "cd ../frontend && npm run build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
